name: Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-slim

    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@v7

      - name: Install dependencies
        run: uv sync

      - name: Test SF feed
        run: uv run drafthouse sf
